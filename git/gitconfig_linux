[credential]
    helper = cache --timeout=43200    
[alias]
# checks out a branch, creates if not exist
    list-branches="!f() {                 \
        git branch -a --color=always      \
        | nl                              \
        | tac;                            \
    }; f"

