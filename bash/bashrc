alias gti=git
alias la='ls -a'
alias ll='ls -l'
alias ne='node -e'
alias vom=vim

alias npmi='rm -rf node_modules package-lock.json; npm install'
alias yarni='rm -rf node_modules yarn.lock package-lock.json; yarn'
alias yarnt='yarn test'

alias adbd='adb devices'
alias adbl='adb logcat'
alias adbll='adb logcat -T1 | grep "ReactNativeJS\\|System\\."'
alias adbr='adb reverse tcp:8081 tcp:8081'
alias rnra='react-native run-android'

alias resource='source ~/.bashrc'

if [ $(uname -s) == "Linux" ]
then
    alias open='xdg-open'
    alias pbcopy='xclip -selection clipboard'
    alias pbpaste='xsel -b'
fi

ssh-port-forward() {
    local port=$1
    shift
    ssh -qL ${port}:127.0.0.1:${port} $@
}

gitb() {
    for a in $@
    do
        git $a
    done
}

# for when you type `gi tstatus` instead of `git status`
gi() {
    local first=$1
    shift
    git ${first:1} $@
}

line() {
    if [ $# -eq 2 ]
    then
        sed "$1q;d" "$2"
    else
        sed "$1q;d"
    fi
}

export HISTCONTROL=ignoreboth:erasedups
